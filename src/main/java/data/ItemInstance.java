package data;

public class ItemInstance {
	//public final TItem item;
	
	private int count = 0;

	public void setOwner(Object character) {
		
	}
	
	public void use() {
		/*
		 * use_codition
		 * equip_condition
		 * item_equip_option
		 * unequip_skill
		 * if(test(owner)) {
		 * 
		 * }
		 * if(template.test(owner)) {
		 *	template.use(owner); 
		 * }
		 */
	}
	
}
